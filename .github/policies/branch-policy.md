# Branch Policy

1. **`main`**: The production branch. Requires signed commits, linear history, and merge queue.
2. **`staging`**: The pre-production branch. Requires QA approval.
3. **`develop`**: The integration branch.
4. **Feature branches**: Must be prefixed with `feature/`, `bugfix/`, `hotfix/`, or `chore/`.
