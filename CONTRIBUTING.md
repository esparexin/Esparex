# Contributing to Esparex

1. We use a GitFlow-lite branch strategy. Branch off `develop`.
2. Follow Conventional Commits for all commit messages.
3. Ensure all tests and governance checks (`npm run governance:all`) pass locally before opening a PR.
4. Keep PRs small and focused. Include screenshots for UI changes.
