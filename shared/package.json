{
  "name": "@esparex/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist *.tsbuildinfo && tsc -b",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  }
}
