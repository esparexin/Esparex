name: listings
description: "Listings Bounded Context: Ad creation, mutation, validation, querying, and lifecycle management."
type: domain
dependencies:
  - catalog
  - identity
  - location
  - payments
  - notifications
