/**
 * ESPAREX — SHIM END-USER AUTH MIDDLEWARE
 * Re-exports the canonical Single Source of Truth from core.
 */
export { protect, extractUser, restrictTo, adminOnly } from "@esparex/core/middleware/authMiddleware";
