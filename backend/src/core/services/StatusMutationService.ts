/**
 * @core barrel — StatusMutationService
 *
 * This file is the @core namespace entry point for StatusMutationService.
 * The canonical implementation lives in backend/src/services/StatusMutationService.ts.
 * This re-export keeps the physical file in place while establishing the @core import boundary.
 *
 * Import as:
 *   import { mutateStatus, mutateStatuses } from '@core/services/StatusMutationService';
 */
export * from '@core/services/StatusMutationService';
