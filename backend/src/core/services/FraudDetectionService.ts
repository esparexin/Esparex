/**
 * @core barrel — FraudDetectionService
 *
 * This file is the @core namespace entry point for FraudDetectionService.
 * The canonical implementation lives in backend/src/services/FraudDetectionService.ts.
 *
 * Import as:
 *   import { analyzeFraudRisk } from '@core/services/FraudDetectionService';
 */
export * from '@core/services/FraudDetectionService';
